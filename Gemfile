source 'https://rubygems.org'

gem 'sinatra', github: 'sinatra/sinatra'
gem 'sinatra-contrib' # includes json lib

group :development, :test do
  gem 'rerun' # for hot reloading
  gem 'pry' # helping for debuggers
  gem 'pry-byebug'
  gem 'pry-nav'
  gem 'minitest' # testing
  gem 'rack-test'
end

